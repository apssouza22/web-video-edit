# Search Tab Integration

Add the search functionality to the left navigation as a new tab with video selection from gallery.

## Implementation Steps

- [x] Add Search tab button to `index.html` (in tab-header)
- [x] Add Search tab content section to `index.html` (in tab-content-wrapper)
- [x] Update `search-view.ts` to include video selector and search input
- [x] Initialize search service in `main.ts`

